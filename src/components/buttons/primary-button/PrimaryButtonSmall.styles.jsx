import styled from 'styled-components';
import PrimaryButton from './PrimaryButton.styles';

const PrimaryButtonSmall = styled.a`
    ${PrimaryButton}
    margin: 0 10px;
    padding: 2px 3px;
`;

export default PrimaryButtonSmall;