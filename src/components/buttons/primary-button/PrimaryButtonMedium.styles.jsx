import styled from 'styled-components';
import PrimaryButton from './PrimaryButton.styles';

const PrimaryButtonMedium = styled.a`
    ${PrimaryButton}
    margin: 0 2rem;
    padding: .2rem 1rem;
`;

export default PrimaryButtonMedium;