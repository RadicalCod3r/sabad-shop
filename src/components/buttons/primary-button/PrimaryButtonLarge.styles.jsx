import styled from 'styled-components';
import PrimaryButton from './PrimaryButton.styles';

const PrimaryButtonLarge = styled.a`
    ${PrimaryButton}
    margin: 0 4rem;
    padding: 0.5rem;
`;

export default PrimaryButtonLarge;